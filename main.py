"""
Main entry point for the Telegram User Adder application.
"""
import sys
import os

# Add the project directory to the Python path
sys.path.insert(0, os.path.abspath(os.path.dirname(__file__)))

def main():
    """Main function to start the application."""
    print("Telegram User Adder starting...")
    # This will be expanded as modules are developed

if __name__ == "__main__":
    main()
